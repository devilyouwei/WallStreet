        
var rowCut = '&nbsp; &#269; test &amp;nbsp; &#236; &nbsp;&nbsp;'

var charArr = [],
            subRow,
            match,
            char;

        for(var j = 0; j < rowCut.length; j++) {

	console.log(j);

            subRow = rowCut.substring(j);
            match = subRow.match(/^&[a-z0-9#]+;/);
            char;

		console.log(match, 'match');

            if (match) {
                char = match[0];

	console.log(char)   

             charArr.push(char);
                j += (char.length - 1);
            } else {
                charArr.push(rowCut[j]);
            }
        }

console.log(rowCut, rowCut.length);
console.log(charArr, charArr.length);
