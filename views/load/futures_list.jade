div
    table.am-table.am-table-bordered.am-table-radius.am-table-striped
        thead
            tr
                th 编号
                th 名称
                th 数据量
                th 开始时间
                th 最终时间
                th 操作
        tbody
            each item in goods
                tr
                    td=item.id
                    td=item.name
                    td=item.count
                    td=item.oldest_date
                    td=item.newest_date
                    td
                        button.train_btn.am-btn.am-btn-sm.am-btn-success(href="/admin/data/train/#{item.id}") 训练
